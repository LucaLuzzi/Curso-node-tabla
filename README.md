# Notas:

Este es mi primer programa en Node.js

```
 Options:
      --help     Show help                                    [boolean]
      --version  Show version number                          [boolean]
  -b, --base     Its the base of the multiplication table     [number][required]
  -l, --listar   Show the table in console                             [boolean]
  -h, --hasta    Limit the multiplication table           [number] [default: 10]
```
